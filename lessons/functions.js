// addition of two numbers
function sum(x, y) {
  const sum = x + y;
  return sum;
}

let result = sum(100, 2);
console.log(result);

// name function
function greet(name) {
  return "Welcome to JavaScript, " + name;
}

let greeting = greet("Wisdom");
console.log(greeting);

greeting = greet("Damilola");
console.log(greeting);

//arrow function
const add = (x, y) => x + y;
result = add(10, 2);
console.log(result);

greet2 = (name) => "Welcome to JavaScript, " + name;
greeting = greet2("Wisdom");
console.log(greeting);


gret3 = (name) => {
  return "Welcome to JavaScript, " + name;
}

sub = (a,b) => {
  const subtraction = b - a;
  return subtraction;
}https://nextgen-academy.slack.com/files/U08JT7ML2TS/F08M2QFUDL3/index.js?origin_team=T03N6P73L4T&origin_channel=D08KN4LLZ24



// Assignment 

1. Create a function that takes two numbers as arguments and returns their product.

2. Create a function that takes a string as an argument and returns the string in uppercase.

3. Create a function that takes a number as an argument and returns the factorial of that number.

4. Create a function that takes an array of numbers as an argument and returns the sum of all the numbers in the array.

5. Create a function that takes an array of strings as an argument and returns the longest string in the array.